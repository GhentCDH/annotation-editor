import type { AnnotationDefConfig } from '@ghentcdh/annotation-core';

export const mockConfiguration: AnnotationDefConfig = {
  baseUrl: 'http://localhost/',
  app: 'playground',
  prefix: 'playground',
  isDev: true,
  cacheTTLms: 0,
};
