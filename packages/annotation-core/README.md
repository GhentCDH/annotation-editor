# annotation-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build annotation-core` to build the library.

## Running unit tests

Run `nx test annotation-core` to execute the unit tests via [Vitest](https://vitest.dev/).
