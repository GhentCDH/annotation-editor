import { ShellMenu } from '@ghentcdh/ui';
export type SidebarLeaf = {
    kind: 'item';
    id: string;
    label: string;
    position?: number;
};
export type SidebarGroup = {
    kind: 'group';
    id: string;
    label: string;
    position?: number;
    children: SidebarLeaf[];
};
export type SidebarNode = SidebarLeaf | SidebarGroup;
export declare const isSidebarGroup: (node: SidebarNode) => node is SidebarGroup;
export declare const isSidebarLeaf: (node: SidebarNode) => node is SidebarLeaf;
export declare const firstLeafId: (nodes: SidebarNode[]) => string | undefined;
/**
 * Flatten SidebarNode[] into a ShellMenu.
 * Groups are inlined (children become top-level entries).
 * Kept for consumers that rely on ShellMenu; AdminView uses SidebarNode[] directly.
 */
export declare const menu: (nodes: SidebarNode[]) => ShellMenu;
//# sourceMappingURL=sidebar.d.ts.map